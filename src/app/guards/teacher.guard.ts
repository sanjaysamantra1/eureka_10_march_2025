// import { CanActivateFn } from '@angular/router';


// export const teacherGuard: CanActivateFn = (route, state) => {
//   const userService = inject(UserService)
//   if (userService = inject(UserService))
//   return true;
// };
