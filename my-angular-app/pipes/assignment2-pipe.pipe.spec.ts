import { Assignment2PipePipe } from './assignment2-pipe.pipe';

describe('Assignment2PipePipe', () => {
  it('create an instance', () => {
    const pipe = new Assignment2PipePipe();
    expect(pipe).toBeTruthy();
  });
});
