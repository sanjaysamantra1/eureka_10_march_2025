import { Assignment3PipePipe } from './assignment3-pipe.pipe';

describe('Assignment3PipePipe', () => {
  it('create an instance', () => {
    const pipe = new Assignment3PipePipe();
    expect(pipe).toBeTruthy();
  });
});
