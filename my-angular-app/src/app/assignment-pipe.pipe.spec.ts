import { AssignmentPipePipe } from './assignment-pipe.pipe';

describe('AssignmentPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AssignmentPipePipe();
    expect(pipe).toBeTruthy();
  });
});
