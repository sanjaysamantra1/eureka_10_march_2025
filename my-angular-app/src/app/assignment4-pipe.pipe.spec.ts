import { Assignment4PipePipe } from './assignment4-pipe.pipe';

describe('Assignment4PipePipe', () => {
  it('create an instance', () => {
    const pipe = new Assignment4PipePipe();
    expect(pipe).toBeTruthy();
  });
});
